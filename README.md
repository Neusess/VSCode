Import this in 'settings.json' in VSCode to change ur UI-Design to what i use  
U don't need to install the extension, but for the full experience i recommend it  
It visually changes VSC to a fully black background with neon colors  
